{
  "id": "timeout-retry",
  "name": "Timeout Retry",
  "version": "0.2.0",
  "description": "Takes over timeout diagnostics before OpenCode starts, retries timeout-aborted model requests, and gives Shell commands at least 10 minutes.",
  "engines": {
    "opencode": "^1.18.0"
  },
  "permissions": ["server.host"],
  "entry": "index.html",
  "contributes": {
    "server": "server.js",
    "serverBootstrap": "bootstrap.js"
  }
}
